import { makeStyles } from "@material-ui/core/styles";
const useStyles = makeStyles({
  root: {
    maxWidth: 400,
    minHeight: 300,
  },
  media: {
    height: 140,
  },
});
export default useStyles;
